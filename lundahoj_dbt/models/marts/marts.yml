version: 2

models:
  - name: dim_stations
    columns:
      - name: station_id
        tests: [not_null, unique]

  - name: fct_readings
    columns:
      - name: observed_at_utc
        tests: [not_null]
